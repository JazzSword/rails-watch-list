<h1><%= @list.name %></h1>

<ul>
<% @movies.each do |movie| %>
  <li><%= movie.title %></li>
<% end %>
</ul>

<%= link_to 'Back', lists_path  %>

<%= link_to 'Add Movie', new_list_bookmark_path(@list)  %>
